///////// LIST

jQuery(function($){    
         
       
});

/*
 * <%--
                          <c:if test="${col.caption.length() > maxCaptionLength}">
                            <c:out value="${col.caption.substring(0, maxCaptionLength)}"/>...
                          </c:if>
                          <c:if test="${col.caption.length() <= maxCaptionLength}">
                            <c:out value="${col.caption}"/>
                          </c:if>
                          --%>

*/